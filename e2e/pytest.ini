[pytest]
pythonpath = . ../pythonlib
testpaths = oracle web drivers journeys live
markers =
    live: real public sites; never on the PR gate
    slow: lifecycle and soak journeys
addopts = -p no:cacheprovider -rs -m "not live"
