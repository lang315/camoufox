# Closing #44, #101, #102 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close three issues on three branches — #102 (a dead version warning made live on the right axis, plus a stale comment), #44 (the headline smoke arm rewritten to guard the documented one-browser-per-OS contract and made blocking), #101 (49 dot-prefixed families removed from the mac font list, with the measurement that justified it turned into a guard).

**Architecture:** Three independent branches off `main` at `367a9db`, three PRs, merged in the order #102 → #44 → #101. #44 and #101 both edit `.github/workflows/smoke.yml` in disjoint regions; a trial merge in a scratch worktree precedes the second PR. Every CI measurement in this plan uses build **34578327006** (branch `fix/95-cmap-unicode-ucs4`, artifact expires 2026-09-25T09:53Z); its binary carries the same patch set as `main` since `f0072fc`.

**Tech Stack:** Python 3.12 (`pythonlib`, pytest), GitHub Actions `smoke.yml` under `xvfb-run`, Playwright 1.55.0, `gh` CLI.

## Global Constraints

Copied from `docs/superpowers/specs/2026-09-12-issues-44-101-102-design.md`, with one correction recorded at the end of this block.

- Branches: `fix/102-bundle-version`, `fix/44-contract-guard`, `fix/101-dot-names`. All off `main`. `fix/101-dot-names` starts from `meas/101-dot-names` (which is `main` + one probe commit).
- All smoke runs: `gh workflow run smoke.yml -R lang315/camoufox --ref <branch> -f run_id=34578327006`.
- Every PR body resolves every quoted run id to its build and that build's branch (CLAUDE.md lesson 9), and quotes the `[launch] fontconfig=... spoofed_os=...` line for every font measurement (lesson 8).
- In a long single-step guard, a failing check is **registered** (`tripwires.append(...)` / `unmeasured_arms[...]`) and triaged at the end of the step — **never** asserted inline (lesson 9's corollary). The #44 arm is its own step and asserts once, at its end, after every reading has printed.
- Retarget any PR whose base is another branch **before** deleting that branch.
- `Version` (`pythonlib/camoufox/pkgman.py:338`) compares on the **build tag** (`beta.31`), not the Firefox version. The Playwright floor is `PLAYWRIGHT_BROWSER_FLOORS = (((1, 61), 'beta.30'),)`.
- `application.ini`'s `[App] Version=` line is `152.0.4-beta.31`: Firefox version, `-`, build tag.
- `fonts.json['mac']`: 574 entries, 49 begin with `.`, none of the 49 is in `_ESSENTIAL_FONTS_MACOS` (16 names). Removal leaves 525.
- **Spec correction (condition 3 of the #44 contract).** The spec says the launch-matching context must produce zero `warn_fonts_excluded_by_launch` warnings. That is wrong: the warning is computed from the set difference `context_fonts − launch_fonts`, and both are independent random 30–78% samples of the same OS list, so a matching context legitimately fires it too — and every prior run shows it firing **three** times, once per context. The correct condition is: **both masked contexts must fire it at least once; the matching context is unconstrained.** Task 2 amends the spec file in its commit.

---

## File map

| File | Task | Responsibility |
|---|---|---|
| `pythonlib/camoufox/utils.py` | 1 | `_bundle_app_version_line()` (shared reader), `_bundle_version()` (new), `_bundle_verstr()` (rewired onto the reader, behaviour unchanged), `warn_if_executable_predates_playwright()` (uses `_bundle_version` first) |
| `pythonlib/tests/test_bundle_verstr.py` | 1 | new tests for `_bundle_version` and for the warning firing / not firing |
| `service-tester/run_tests.py:178-183` | 1 | comment correction |
| `.github/workflows/smoke.yml` (#44 step, currently lines 6957–7185 on `main`) | 2 | contract guard rewrite |
| `docs/superpowers/specs/2026-09-12-issues-44-101-102-design.md` | 2 | condition-3 correction |
| `pythonlib/camoufox/fonts.json` | 5 | 49 deletions on line 6 |
| `.github/workflows/smoke.yml` (#101 probe, inside the step starting line 602) | 5 | deferred assert added to the probe |

---

### Task 1: `_bundle_version()` and a live `warn_if_executable_predates_playwright` (#102)

**Branch:** `git checkout -b fix/102-bundle-version main`

**Files:**
- Modify: `pythonlib/camoufox/utils.py:125-208` (the warn function and `_bundle_verstr`)
- Modify: `pythonlib/tests/test_bundle_verstr.py` (append)
- Modify: `service-tester/run_tests.py:178-183`

**Interfaces:**
- Consumes: `Version` (already imported in `utils.py` from `.pkgman`; constructed as `Version(build=..., version=...)`), `effective_version_min()`, `pkgman._resolved_playwright_version()`.
- Produces: `_bundle_app_version_line(path: Optional[Path]) -> Optional[str]` (raw value after `Version=`, stripped, or `None`); `_bundle_version(path: Optional[Path]) -> Optional[Version]`.

- [ ] **Step 1: Write the failing tests**

Append to `pythonlib/tests/test_bundle_verstr.py`:

```python
import warnings  # noqa: E402  (place with the other imports at the top of the file)
import pytest  # noqa: E402

from camoufox import utils as camoufox_utils  # noqa: E402
from camoufox import pkgman  # noqa: E402
from camoufox.pkgman import Version  # noqa: E402
from camoufox.utils import _bundle_version  # noqa: E402


# --- _bundle_version: both halves of `Version=`, on the axis Version compares --

def test_bundle_version_reads_build_tag_and_firefox_version(tmp_path):
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    (tmp_path / "application.ini").write_bytes(APP_INI)

    v = _bundle_version(exe)
    assert v is not None
    assert v.build == "beta.31"
    assert v.version == "152.0.4"
    # The comparison Version actually makes is on the build tag.
    assert v >= Version(build="beta.30")
    assert v > Version(build="beta.29")


def test_bundle_version_macos_app_layout(tmp_path):
    contents = tmp_path / "Camoufox.app" / "Contents"
    exe = contents / "MacOS" / "camoufox"
    exe.parent.mkdir(parents=True)
    exe.write_bytes(b"")
    (contents / "Resources").mkdir(parents=True)
    (contents / "Resources" / "application.ini").write_bytes(APP_INI)

    v = _bundle_version(exe)
    assert v is not None and v.build == "beta.31"


def test_bundle_version_without_build_tag_returns_none(tmp_path):
    # A `Version=` with no `-<build>` half cannot be placed on the floor axis.
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    (tmp_path / "application.ini").write_bytes(b"[App]\nVersion=152.0.4\n")

    assert _bundle_version(exe) is None


def test_bundle_version_missing_file_returns_none(tmp_path):
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    assert _bundle_version(exe) is None


def test_bundle_version_none_path_returns_none():
    assert _bundle_version(None) is None


# --- warn_if_executable_predates_playwright: now reachable for packages -------

def _pin_playwright(monkeypatch, major_minor):
    """effective_version_min() reads the resolved Playwright; pin it so the floor
    is deterministic regardless of what is installed on the host."""
    monkeypatch.setattr(pkgman, "_resolved_playwright_version", lambda: major_minor)


def test_warning_fires_for_a_package_below_the_floor(tmp_path, monkeypatch):
    """#102: packages ship no version.json, so this warning never fired for any
    real user. With application.ini as the source it fires when it should."""
    _pin_playwright(monkeypatch, (1, 61))  # floor becomes beta.30
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    (tmp_path / "application.ini").write_bytes(
        b"[App]\nVersion=152.0.0-beta.29\n"
    )

    with pytest.warns(RuntimeWarning, match=r"is beta\.29, but Playwright .* needs at least beta\.30"):
        camoufox_utils.warn_if_executable_predates_playwright(exe)


def test_warning_silent_for_a_package_at_or_above_the_floor(tmp_path, monkeypatch):
    _pin_playwright(monkeypatch, (1, 61))
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    (tmp_path / "application.ini").write_bytes(APP_INI)  # beta.31

    with warnings.catch_warnings(record=True) as rec:
        warnings.simplefilter("always")
        camoufox_utils.warn_if_executable_predates_playwright(exe)
    assert [w for w in rec if "needs at least" in str(w.message)] == []


def test_warning_silent_when_playwright_is_below_the_pairing(tmp_path, monkeypatch):
    # Playwright < 1.61 never sends the fields beta.29 rejects; no floor is raised.
    _pin_playwright(monkeypatch, (1, 60))
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    (tmp_path / "application.ini").write_bytes(b"[App]\nVersion=152.0.0-beta.29\n")

    with warnings.catch_warnings(record=True) as rec:
        warnings.simplefilter("always")
        camoufox_utils.warn_if_executable_predates_playwright(exe)
    assert [w for w in rec if "needs at least" in str(w.message)] == []


def test_version_json_still_answers_when_no_application_ini(tmp_path, monkeypatch):
    # An unpackaged objdir build with version.json keeps the pre-#102 path.
    _pin_playwright(monkeypatch, (1, 61))
    exe = tmp_path / "camoufox"
    exe.write_bytes(b"")
    (tmp_path / "version.json").write_bytes(b'{"version": "152.0.0", "release": "beta.29"}')

    with pytest.warns(RuntimeWarning, match=r"is beta\.29"):
        camoufox_utils.warn_if_executable_predates_playwright(exe)


_REAL_BINARY = "/tmp/cf97/cf/Camoufox.app/Contents/MacOS/camoufox"


@pytest.mark.skipif(not os.path.exists(_REAL_BINARY), reason="extracted package not present")
def test_real_package_does_not_warn(monkeypatch):
    """The regression that matters: if parsing the beta suffix misreads a
    current package as below the floor, every executable_path user gets a
    spurious warning on every launch. Read against a real extracted build."""
    _pin_playwright(monkeypatch, (1, 61))
    with warnings.catch_warnings(record=True) as rec:
        warnings.simplefilter("always")
        camoufox_utils.warn_if_executable_predates_playwright(_REAL_BINARY)
    assert [w for w in rec if "needs at least" in str(w.message)] == []
```

- [ ] **Step 2: Run the new tests to verify they fail**

Run: `cd pythonlib && python -m pytest tests/test_bundle_verstr.py -q 2>&1 | tail -15`

Expected: the five `_bundle_version` tests fail with `ImportError: cannot import name '_bundle_version'` (collection error, so the whole file errors — that is the RED; note it). Temporarily comment out the `_bundle_version` import line to see the warning tests individually: `test_warning_fires_for_a_package_below_the_floor` FAILS with `DID NOT WARN` (the function returns at the `FileNotFoundError` from `Version.from_path`); `test_version_json_still_answers_when_no_application_ini` PASSES already (pre-existing path). Restore the import.

- [ ] **Step 3: Implement — shared reader, `_bundle_version`, rewire `_bundle_verstr`**

In `pythonlib/camoufox/utils.py`, replace the body of `_bundle_verstr` (keep its docstring) and add the two functions **immediately before it**:

```python
def _bundle_app_version_line(path: Optional[Path]) -> Optional[str]:
    """The raw value of `[App] Version=` from the bundle's own application.ini.

    Packages ship application.ini -- a Gecko invariant generated from
    build/application.ini.in -- but not version.json, which the installer
    writes into INSTALL_DIR. On macOS the binary sits in Contents/MacOS and
    the file in Contents/Resources; both layouts are tried. Only [App] carries
    a bare `Version=` ([Gecko] uses MinVersion=/MaxVersion=), and [App] is the
    first section, so no section tracking is needed. Returns None -- never
    raises -- on no path, no file, or no such line.
    """
    if path is None:
        return None
    parent = Path(path).parent
    for ini in (
        parent / "application.ini",
        parent.parent / "Resources" / "application.ini",
    ):
        try:
            text = ini.read_text(encoding="utf-8", errors="replace")
        except OSError:
            continue
        for line in text.splitlines():
            if line.startswith("Version="):
                return line[len("Version="):].strip()
    return None


def _bundle_version(path: Optional[Path]) -> Optional[Version]:
    """The build at `path` as a Version, on the axis Version compares.

    Version.__lt__/__eq__ compare `sorted_rel`, which is derived from the BUILD
    TAG (`beta.31`) -- not the Firefox version. PLAYWRIGHT_BROWSER_FLOORS is
    keyed the same way (`beta.30`). application.ini's `Version=` carries both
    halves as `<firefox>-<build>`; this splits them so the result can stand in
    for Version.from_path() where a package has no version.json (#102).

    Returns None when there is no parsable line or the value has no `-<build>`
    half: a version with no build tag cannot be placed on the floor axis, and
    guessing one would produce a confident wrong answer.
    """
    value = _bundle_app_version_line(path)
    if value is None:
        return None
    version, sep, build = value.partition("-")
    if not sep or not build:
        return None
    return Version(build=build, version=version)
```

Then make `_bundle_verstr`'s body:

```python
    value = _bundle_app_version_line(path)
    if value is None:
        return None
    major = value.split('.', 1)[0]
    return major if major.isdigit() else None
```

(Its eight existing tests must stay green unchanged; `test_gecko_minversion_is_not_mistaken_for_the_app_version` and `test_unparsable_version_line_returns_none` cover the `None` paths through the shared reader.)

- [ ] **Step 4: Implement — `warn_if_executable_predates_playwright` uses the bundle first**

Replace the block

```python
    if path is None:
        return
    try:
        installed = Version.from_path(Path(path).parent)
    except (FileNotFoundError, KeyError, ValueError):
        return
```

with

```python
    if path is None:
        return
    # Packages ship application.ini but no version.json, so for every real
    # executable_path the version.json read below raised FileNotFoundError and
    # this function silently returned -- it had never fired for anyone (#102).
    installed = _bundle_version(path)
    if installed is None:
        try:
            installed = Version.from_path(Path(path).parent)
        except (FileNotFoundError, KeyError, ValueError):
            return
```

and change the docstring's last paragraph from

```
    A build with no version.json beside it -- an unpackaged objdir build, say --
    tells us nothing, so it is left alone.
```

to

```
    The version comes from the bundle's own application.ini (what packages
    ship), falling back to version.json (what an unpackaged objdir build with a
    managed-style layout might carry). A build with neither tells us nothing,
    so it is left alone.
```

- [ ] **Step 5: Run the whole file to verify it passes**

Run: `cd pythonlib && python -m pytest tests/test_bundle_verstr.py -v 2>&1 | tail -25`

Expected: `18 passed` (8 pre-existing + 10 new; `test_real_package_does_not_warn` runs only if `/tmp/cf97` is present — report which). Then the suite: `python -m pytest tests/ -q 2>&1 | tail -3` — expected the same pre-existing failure count as `main` (8 failed, 9 skipped, per the #97 review) with the passed count up by 10. Report both numbers.

- [ ] **Step 6: Correct the service-tester comment**

In `service-tester/run_tests.py`, replace lines 178–183:

```python
    # The launch-level font list becomes a process-wide whitelist:
    # gfxPlatformFontList's constructor writes it to font.system.whitelist and
    # ApplyWhitelist() then DELETES every other family from mFontFamilies. A
    # per-context setFontList() runs later and can only narrow what survived, so
    # a macOS profile inside a browser launched as Windows can never get its
    # Apple families back -- measured 0/3 vs 3/3 marker fonts (#44/#45).
```

with

```python
    # The launch-level font list is a process-wide mask, enforced at lookup
    # time as an AND-gate (patches/font-hijacker.patch, header): a family the
    # launch list does not name is refused wherever a font is looked up, and a
    # per-context setFontList() can only narrow that further, never widen it.
    # (An earlier version of the patch deleted the families outright via
    # ApplyWhitelist(); the outcome is the same, the mechanism is not.) So a
    # macOS profile inside a browser launched as Windows can never get its
    # Apple families back -- measured 0/3 vs 3/3 marker fonts (#44/#45).
```

- [ ] **Step 7: Commit**

```bash
git add pythonlib/camoufox/utils.py pythonlib/tests/test_bundle_verstr.py service-tester/run_tests.py
git commit -m "fix(pythonlib): make the Playwright-floor warning fire for packaged builds (#102)

warn_if_executable_predates_playwright read version.json, which no published
package ships, so for every real executable_path it hit FileNotFoundError and
returned silently: the warning had never fired for anyone. It now reads the
build from application.ini first, through a new _bundle_version().

The obvious swap -- reuse _bundle_verstr() -- is wrong: Version compares on
the build tag (beta.31), the same axis PLAYWRIGHT_BROWSER_FLOORS uses, and
_bundle_verstr returns the Firefox major. application.ini's Version= line
carries both halves as <firefox>-<build>; _bundle_version splits them. Both
readers share _bundle_app_version_line(). version.json remains the fallback.

Tests: red first (DID NOT WARN on a beta.29 fake), then green; a beta.31 fake
and the real extracted package both stay silent under Playwright 1.61, so a
current package cannot produce a spurious warning.

Also corrects the service-tester comment that described masking as
ApplyWhitelist() deleting families; the shipped mechanism is a lookup-time
AND-gate per the font-hijacker patch header."
git push -u origin fix/102-bundle-version
```

---

### Task 2: Rewrite the #44 headline arm as a contract guard, blocking (#44)

**Branch:** `git checkout -b fix/44-contract-guard main`

**Files:**
- Modify: `.github/workflows/smoke.yml` — the step `- name: "NewContext(os=X) is cross-signal coherent (#44 headline)"` (lines 6957–7185 on `main`; find by name, not line number). Replace the whole step, from its `- name:` line through the line before the next `# Bulk dump of what the per-context font-list guard captured` comment.
- Modify: `docs/superpowers/specs/2026-09-12-issues-44-101-102-design.md` — condition 3.

**Interfaces:**
- Consumes: `AsyncCamoufox(from_options=..., os=..., headless=..., executable_path=...)`, `AsyncNewContext(browser, os=...)`, `launch_options(...)`, `_reassemble_camou_config(opts) -> dict|None`, `get_target_os(config) -> 'macos'|'windows'|'linux'`, config key `webGl:renderer`, warning message substring `"can only narrow it, never restore a family (#44)"`.
- Produces: a blocking step named `"mixed-OS contexts from one launch honour the launch font mask, warn, and keep WebGL per-context (#44 contract guard)"`.

- [ ] **Step 1: Replace the step**

Delete the existing step in full and insert:

```yaml
      - name: "mixed-OS contexts from one launch honour the launch font mask, warn, and keep WebGL per-context (#44 contract guard)"
        timeout-minutes: 15
        # This step used to be the "#44 headline" measurement, marked
        # continue-on-error because it asserted the UNSUPPORTED behaviour --
        # that every context resolves its own OS's fonts from one launch --
        # and was known red (runs 34618162525, 34620762155, 34656256911,
        # 34658431749, 34678025310: five runs, three launch draws, same result).
        #
        # That behaviour is documented as unsupported. Since 8f36f9b (#45/#73)
        # pythonlib warns at NewContext time that the launch font list is a
        # process-wide mask a per-context list can only narrow, and tells the
        # caller to launch one browser per OS. So the contract is:
        #   (1) the context whose os= matches the LAUNCH's spoofed OS resolves
        #       its required marker fonts;
        #   (2) the other two contexts resolve NO markers -- not their own (the
        #       mask holds) and not anyone else's (no cross-OS leak);
        #   (3) warn_fonts_excluded_by_launch fires for each of those two (the
        #       matching context may or may not fire it -- both sides are random
        #       30-78% samples of the same list, so its set difference can be
        #       non-empty too -- and is not asserted);
        #   (4) WebGL stays per-context: all three contexts reporting the
        #       launch's renderer is the original #44 defect.
        # This step guards that contract and is BLOCKING.
        #
        # Marker validity is established here, deterministically: one explicit
        # launch per OS before the headline launch. The old arm depended on a
        # random draw to ever exercise the lin set; this does not.
        run: |
          # Entry point #44's body names: AsyncCamoufox() with NO os= at launch,
          # then three contexts from that ONE browser with three different os=.
          # `opts` is precomputed via launch_options() and reused through
          # `from_options=` so the printed FONTCONFIG_FILE / spoofed OS are read
          # back from the launch that actually ran (CLAUDE.md lesson 6).
          #
          # No ff_version is passed; on a runner with no managed install that
          # used to raise (#97), so reaching the browser is #97's end-to-end proof.
          #
          # xvfb is mandatory: headless returns '(no webgl)' for every renderer.
          pip install --quiet -e pythonlib 'playwright==1.55.0'
          xvfb-run -a --server-args="-screen 0 1920x1080x24" python - <<'PY'
          import asyncio, os, warnings
          from camoufox.async_api import AsyncCamoufox, AsyncNewContext
          from camoufox.utils import (_reassemble_camou_config, get_target_os,
                                       launch_options)

          READ = '''() => {
            const c = document.createElement('canvas');
            const gl = c.getContext('webgl');
            const dbg = gl && gl.getExtension('WEBGL_debug_renderer_info');
            const renderer = dbg ? gl.getParameter(dbg.UNMASKED_RENDERER_WEBGL)
                                 : '(no webgl)';
            const probe = (f) => {
              const s = document.createElement('span');
              s.style.cssText = 'position:absolute;font-size:72px';
              s.textContent = 'mmmmmmmmwwwwwww';
              document.body.appendChild(s);
              s.style.fontFamily = 'monospace';
              const base = s.getBoundingClientRect().width;
              s.style.fontFamily = `"${f}", monospace`;
              const w = s.getBoundingClientRect().width;
              s.remove();
              return w !== base;
            };
            // Markers must be (1) bundled by Camoufox for exactly this OS --
            // NOT a host font the CI runner happens to have -- and (2) not
            // that OS's own `monospace` generic alias, or the probe compares
            // a face against itself and reads ABSENT although present.
            // DejaVu Sans / Liberation Sans / Ubuntu are the runner's host
            // fonts, invisible under FONTCONFIG_FILE; Menlo/Consolas are the
            // mac/win monospace aliases; Monaco is monospaced at 600/1000em
            // next to Menlo's 602 and collapses to the same width at 72px.
            // PingFang HK (855/755) replaced it. Cambria is NOT in
            // _ESSENTIAL_FONTS_WINDOWS, so it is in the random sample and is
            // never required -- see REQUIRED below.
            //
            // probe() returns `w !== base`, so a dead layout reads
            // base === w === 0 for every family, absent reference included --
            // byte-identical to a live context refusing every marker. The
            // baseline is returned so the two can be told apart (lesson 4).
            const baseWidth = (() => {
              const s = document.createElement('span');
              s.style.cssText = 'position:absolute;font-size:72px';
              s.textContent = 'mmmmmmmmwwwwwww';
              document.body.appendChild(s);
              s.style.fontFamily = 'monospace';
              const b = s.getBoundingClientRect().width;
              s.remove();
              return b;
            })();
            return {
              platform: navigator.platform,
              renderer,
              base: baseWidth,
              mac: ['Helvetica Neue', 'Lucida Grande', 'PingFang HK'].filter(probe),
              win: ['Segoe UI', 'Calibri', 'Cambria'].filter(probe),
              lin: ['Arimo', 'Tinos', 'Noto Sans JP'].filter(probe),
              absent: probe('__CamouAbsentRef__0001'),
            };
          }'''

          EXPECT = {'macos': 'mac', 'windows': 'win', 'linux': 'lin'}
          # Markers a context of that OS must resolve when nothing masks it.
          # Cambria is a random-sample font (not essential) and is excluded.
          REQUIRED = {
              'mac': ['Helvetica Neue', 'Lucida Grande', 'PingFang HK'],
              'win': ['Segoe UI', 'Calibri'],
              'lin': ['Arimo', 'Tinos', 'Noto Sans JP'],
          }
          NARROW = "can only narrow it, never restore a family (#44)"
          BIN = os.environ['CAMOUFOX_BIN']

          async def read_context(browser, os_name):
              # warn_fonts_excluded_by_launch is called inside AsyncNewContext,
              # in this coroutine, after the executor-side fingerprint draw
              # returns (async_api.py:230) -- so a catch_warnings around the
              # await captures it.
              with warnings.catch_warnings(record=True) as rec:
                  warnings.simplefilter("always")
                  ctx = await AsyncNewContext(browser, os=os_name)
              fired = sum(1 for w in rec if NARROW in str(w.message))
              page = await ctx.new_page()
              await page.goto('data:text/html,<h1>c</h1>')
              r = await page.evaluate(READ)
              await ctx.close()
              r['warned'] = fired
              return r

          # -- precheck: every marker set resolves under its own OS ------------
          # One explicit launch per OS, context of the same OS. If a required
          # marker does not resolve HERE, the headline cannot read it either,
          # and any empty list below would be the probe, not the contract.
          async def precheck():
              out = {}
              for os_name in ('macos', 'windows', 'linux'):
                  async with AsyncCamoufox(os=os_name, headless=False,
                                           executable_path=BIN) as b:
                      out[os_name] = await read_context(b, os_name)
              return out

          pre = asyncio.run(precheck())
          invalid = []
          for os_name, r in pre.items():
              own = EXPECT[os_name]
              print(f"  [precheck {os_name}] {own}={r[own]} base={r['base']} absent={r['absent']}")
              missing = [f for f in REQUIRED[own] if f not in r[own]]
              if missing:
                  invalid.append(f"precheck {os_name}: required {own} markers did not resolve "
                                 f"under their own launch: {missing}")
              if r['absent']:
                  invalid.append(f"precheck {os_name}: the absent reference resolved")
              if r['base'] <= 0:
                  invalid.append(f"precheck {os_name}: probe laid out nothing (base={r['base']})")

          # -- headline: one launch, no os=, three contexts --------------------
          opts = launch_options(headless=False, executable_path=BIN)
          fontconfig_file = opts['env'].get('FONTCONFIG_FILE', '(unset)')
          launch_config = _reassemble_camou_config(opts)
          if launch_config is None:
              invalid.append("launch config did not reassemble from CAMOU_CONFIG_* -- cannot "
                             "read the launch's OS or renderer")
              spoofed_os, launch_renderer = '(unavailable)', None
          else:
              spoofed_os = get_target_os(launch_config)
              launch_renderer = launch_config.get('webGl:renderer')
          print(f"  [launch] fontconfig={fontconfig_file} spoofed_os={spoofed_os} "
                f"renderer={launch_renderer}")

          async def headline():
              seen = {}
              async with AsyncCamoufox(from_options=opts) as browser:
                  for os_name in ('macos', 'windows', 'linux'):
                      seen[os_name] = await read_context(browser, os_name)
              return seen

          seen = asyncio.run(headline())
          for os_name, r in seen.items():
              print(f"  [{os_name}] platform={r['platform']} renderer={r['renderer']}")
              print(f"  [{os_name}] mac={r['mac']} win={r['win']} lin={r['lin']} "
                    f"absent={r['absent']} base={r['base']} warned={r['warned']}")

          # -- setup validity (lesson 4) ----------------------------------------
          if any(r['renderer'] == '(no webgl)' for r in seen.values()):
              invalid.append("no GL context -- every renderer read is '(no webgl)'")
          if any(r['absent'] for r in seen.values()):
              invalid.append("the deliberately-absent family measured as present")
          if len({r['platform'] for r in seen.values()}) != 3:
              invalid.append(f"contexts did not differ on navigator.platform: "
                             f"{[r['platform'] for r in seen.values()]}")
          if any(r['base'] <= 0 for r in seen.values()):
              invalid.append(f"a context laid out nothing (base widths "
                             f"{[r['base'] for r in seen.values()]})")
          launch_key = EXPECT.get(spoofed_os)
          if launch_key is None:
              invalid.append(f"launch spoofed_os={spoofed_os!r} is not one of {list(EXPECT)}")
          renderers = [r['renderer'] for r in seen.values()]
          all_launch = launch_renderer is not None and all(x == launch_renderer for x in renderers)
          # Two contexts drawing the same WebGL renderer is a coincidence, not
          # the #44 defect (that is all three == launch). Context renderers are
          # sampled from webgl/webgl_data.db (AsyncNewContext passes no preset);
          # its mac and lin pools share 3 strings, so on a weighted draw
          # it happens on roughly 1 run in 37. It must NOT go through `invalid`: that exits before the font
          # verdict, and the font half is what this guard exists for. Only the
          # WebGL sub-verdict is skipped; the font contract is still measured.
          webgl_measurable = all_launch or len(set(renderers)) == 3
          if not webgl_measurable:
              print(f"::notice::WebGL preset collision between contexts: {sorted(renderers)} "
                    f"(launch={launch_renderer}) -- WebGL half not evaluated this run")
          if invalid:
              print("::warning::SETUP-INVALID -- this guard measured nothing:")
              for line in invalid:
                  print(f"  - {line}")
              raise SystemExit("setup-invalid")

          # -- contract verdict --------------------------------------------------
          bad = []
          for os_name, r in seen.items():
              own = EXPECT[os_name]
              if own == launch_key:
                  missing = [f for f in REQUIRED[own] if f not in r[own]]
                  if missing:
                      bad.append(f"{os_name} (matches launch {spoofed_os}): required markers "
                                 f"missing {missing} -- the launch mask refused its own OS")
              else:
                  for k in ('mac', 'win', 'lin'):
                      if r[k]:
                          bad.append(f"{os_name} (masked by launch {spoofed_os}): resolved {k} "
                                     f"markers {r[k]} -- expected none")
                  if r['warned'] == 0:
                      bad.append(f"{os_name} (masked by launch {spoofed_os}): "
                                 f"warn_fonts_excluded_by_launch did not fire")
          if all_launch:
              bad.append(f"every context reports the LAUNCH renderer {launch_renderer!r} "
                         f"-- the original #44 WebGL defect")

          assert not bad, "#44 contract violated:\n  " + "\n  ".join(bad)
          print(f"  #44 contract holds: launch={spoofed_os}, matching context resolved "
                f"{REQUIRED[launch_key]}, both masked contexts empty and warned, "
                f"three distinct renderers")
          PY
        env:
          CAMOUFOX_BIN: ${{ steps.bin.outputs.bin }}
          LIBGL_ALWAYS_SOFTWARE: "1"
```

- [ ] **Step 2: Validate YAML and confirm the step boundary**

Run:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/smoke.yml')); print('yaml ok')"
grep -n 'continue-on-error' .github/workflows/smoke.yml   # expect: no hit inside this step
grep -n '#44 contract guard' .github/workflows/smoke.yml   # expect: the new name, once
grep -n 'Bulk dump of what the per-context font-list guard captured' .github/workflows/smoke.yml  # still present, directly after the step
```

- [ ] **Step 3: Amend the spec's condition 3**

In `docs/superpowers/specs/2026-09-12-issues-44-101-102-design.md`, replace:

```
3. **`warn_fonts_excluded_by_launch` fired for exactly the two non-matching
   contexts.** Captured with `warnings.catch_warnings(record=True)` +
   `warnings.simplefilter("always")` around each `AsyncNewContext` call; match on
   the message substring `"can only narrow it, never restore a family (#44)"`.
   The matching context must produce zero such warnings.
```

with:

```
3. **`warn_fonts_excluded_by_launch` fired for each of the two non-matching
   contexts.** Captured with `warnings.catch_warnings(record=True)` +
   `warnings.simplefilter("always")` around each `AsyncNewContext` call; match on
   the message substring `"can only narrow it, never restore a family (#44)"`.
   The matching context is **not** asserted either way: the warning is computed
   from `context_fonts − launch_fonts`, and both are independent random 30–78%
   samples of the same OS list, so a matching context legitimately fires it too
   (every prior run shows three firings, one per context). An earlier revision of
   this spec required zero for the matching context; that was wrong.
```

- [ ] **Step 4: Commit and push**

```bash
git add .github/workflows/smoke.yml docs/superpowers/specs/2026-09-12-issues-44-101-102-design.md
git commit -m "test(smoke): guard the one-browser-per-OS contract instead of measuring its violation (#44)

The #44 headline arm asserted that three contexts from one launch each resolve
their own OS's fonts. That is the unsupported case: since 8f36f9b (#45/#73)
pythonlib warns at NewContext time that the launch font list is a process-wide
mask a per-context list can only narrow, and says to launch one browser per OS.
Five runs on three launch draws showed the same rule every time, so the arm was
continue-on-error and known red -- a measurement, not a guard.

It now asserts the documented contract and is blocking: the context matching
the launch's spoofed OS resolves its required markers; the two masked contexts
resolve no markers at all, theirs or anyone's, and each trips
warn_fonts_excluded_by_launch; and no context reports the launch's WebGL
renderer (all three doing so is the original #44 defect). Two contexts sharing
a non-launch renderer is a preset collision: the WebGL sub-verdict is skipped
with a ::notice:: and the font verdict still runs (routing it through
setup-invalid would exit before the font half, on ~1 run in 37).

Marker validity is no longer left to a random draw: one explicit launch per OS
precedes the headline and requires that OS's markers to resolve under its own
mask, so the lin set -- never observed resolving in any prior run -- is now
checked on every run. Cambria stays out of REQUIRED because it is sampled, not
essential.

The spec's condition 3 required the matching context to fire zero warnings;
that was wrong (the warning is a set difference of two random samples of the
same list) and is corrected in the same commit."
git push -u origin fix/44-contract-guard
```

---

### Task 3: Evidence runs for the #44 guard — two greens, two draws, one demonstrated red

**Branch:** `fix/44-contract-guard` (dispatch only; the RED uses a scratch branch)

**Files:** none modified on the branch. Creates and deletes `scratch/44-red`.

**Interfaces:**
- Consumes: the step from Task 2. Prints `[precheck <os>] ...`, `[launch] fontconfig=... spoofed_os=... renderer=...`, `[<os>] ... warned=N`, and `#44 contract holds: ...`.
- Produces: run ids and quoted lines for the PR body in Task 6.

- [ ] **Step 1: Dispatch until two distinct launch draws are green**

```bash
gh workflow run smoke.yml -R lang315/camoufox --ref fix/44-contract-guard -f run_id=34578327006
# wait ~15 min; then:
RUN=$(gh run list -R lang315/camoufox --workflow=smoke.yml -b fix/44-contract-guard -L 1 --json databaseId --jq '.[0].databaseId')
JOB=$(gh run view $RUN -R lang315/camoufox --json jobs --jq '.jobs[0].databaseId')
gh api --allow-escape-sequences "repos/lang315/camoufox/actions/jobs/$JOB/logs" > /tmp/g44-$RUN.log
gh run view $RUN -R lang315/camoufox --json conclusion --jq .conclusion
grep -o 'gh run download "[0-9]*"' /tmp/g44-$RUN.log | head -1     # must be 34578327006
python3 - "$RUN" <<'PY'
import re, sys
esc = re.compile(r'\x1b\[[0-9;]*m')
for line in open(f'/tmp/g44-{sys.argv[1]}.log', errors='replace'):
    if '\x1b[36;1m' in line: continue
    s = re.sub(r'^\S+Z ', '', esc.sub('', line).rstrip())
    if any(k in s for k in ('[precheck', '[launch]', '] platform=', '] mac=', '#44 contract', 'SETUP-INVALID', 'contract violated')):
        print(s.strip())
PY
```

Expected per run: conclusion `success`; three `[precheck ...]` lines each listing all required markers (the `linux` one shows `lin=['Arimo', 'Tinos', 'Noto Sans JP']` — this is the first observation of the lin set resolving); one `[launch]` line; the matching context's list non-empty, the other two `mac=[] win=[] lin=[]` with `warned=` ≥ 1; the closing `#44 contract holds` line. Repeat the dispatch until two green runs show **different** `spoofed_os=` values (at most four dispatches; each is independent). Record every run id, its conclusion and its `spoofed_os` in the ledger.

- [ ] **Step 2: Demonstrate the guard can go red**

```bash
git checkout -b scratch/44-red fix/44-contract-guard
python3 - <<'PY'
from pathlib import Path
p = Path('.github/workflows/smoke.yml'); t = p.read_text()
old = """                  for k in ('mac', 'win', 'lin'):
                      if r[k]:
                          bad.append(f"{os_name} (masked by launch {spoofed_os}): resolved {k} \""""
new = """                  for k in ('mac', 'win', 'lin'):
                      if not r[k]:  # SCRATCH: inverted to prove the guard goes red
                          bad.append(f"{os_name} (masked by launch {spoofed_os}): resolved {k} \""""
assert t.count(old) == 1; p.write_text(t.replace(old, new)); print('inverted')
PY
git commit -qam "scratch: invert the masked-context condition to prove the #44 guard goes red" && git push -u origin scratch/44-red
gh workflow run smoke.yml -R lang315/camoufox --ref scratch/44-red -f run_id=34578327006
```

After it completes: conclusion must be `failure`; the log must contain `AssertionError: #44 contract violated:` followed by `(masked by launch ...): resolved ... markers [] -- expected none` lines. Quote the assertion block. Then `git checkout fix/44-contract-guard && git branch -D scratch/44-red && git push origin --delete scratch/44-red`. Record the run id as the demonstrated RED.

- [ ] **Step 3: Record in the ledger**

Append to `.superpowers/sdd-44-101-102/progress.md`: each run id → build 34578327006 → branch `fix/95-cmap-unicode-ucs4`, its conclusion, its `spoofed_os`, and the three precheck lines from one run verbatim.

---

### Task 4: Close #44; file the context-0 fail-open issue

**Files:** none in the repo.

- [ ] **Step 1: File the fail-open issue**

```bash
cat > /tmp/i-ctx0.md <<'EOF'
`CamouIsFontAllowed` treats a context id of 0 as "no per-context list" and returns `true`, allowing every family. The id reaches it through four hops from `gfxFontGroup`'s constructor — `mFontVisibilityProvider->GetDocument()` → inner window → `BrowsingContext` — and each hop fails silently to 0. So any path that cannot establish which context is asking gets the whole font list, and the launch-level mask is the only thing left in the way.

This is CLAUDE.md lesson 5, verbatim in spirit: "a gate that cannot establish who is asking should deny." No round of the fonts work has changed it. Two hops have already been found failing (`OffscreenCanvas::GetDocument()` off-main-thread, and the #83 cache), and fixing individual hops does not close the class.

Evidence that the fail-open is live on a current build: in every #95 control run, arm j2's bare context (`CAMOU_CONFIG={}`, no `setFontList`) resolves U+FFFD global fallback to `.LastResort` — a family no list would grant — because the gate answers `true` for context 0. See PR #98's control section and its comment on the accepted trade-off.

Opened so that closing #44 (which accepted "one browser per OS" for its fonts half) does not drop this thread. No fix is proposed here; the design question is whether the gate should deny on an unresolved context, and what that breaks.
EOF
gh issue create -R lang315/camoufox --title "Font gate fails open for context id 0 (CLAUDE.md lesson 5)" --body-file /tmp/i-ctx0.md
```

Record the new issue number.

- [ ] **Step 2: Close #44 with the decision on record**

Write `/tmp/c44.md` (fill in the run ids from Task 3 and the issue number from Step 1):

```
Closing, with the fonts half accepted as documented behaviour rather than fixed.

**WebGL half:** fixed and measured — three distinct, platform-coherent renderers across five runs of the headline arm (34618162525, 34620762155, 34656256911, 34658431749, 34678025310; all consumed build 34578327006, branch `fix/95-cmap-unicode-ucs4`, the last via `main`'s merge of it).

**Fonts half:** the launch-level font list is a process-wide mask that a per-context `setFontList()` can only narrow. This has been the product's contract since `8f36f9b` (2026-08-28, *"fix(#45): one browser per OS, and stop masking the font failures it caused (#73)"*), and `warn_fonts_excluded_by_launch` says so at `NewContext` time with the workaround attached: launch one browser per OS, or pass the same `os=` at launch. `service-tester` already does exactly that. The title's "silently" has not been true for fonts since that commit.

The alternative — making the mask per-context in C++ — was considered and not taken: it widens a gate that has had three rounds of work and still fails open at context 0, for a scenario the product already tells the caller not to use. That fail-open is now tracked separately as #<NEW>.

**What changed on `main`:** PR #<44-PR> rewrites the headline arm from a known-red measurement of the unsupported case into a blocking guard for the contract: the launch-matching context resolves its markers, the two masked contexts resolve nothing and each trips the warning, and WebGL stays per-context. Marker validity is established by an explicit per-OS launch on every run, so it no longer waits on a random draw. Green on runs <RUN-A> (`spoofed_os=<A>`) and <RUN-B> (`spoofed_os=<B>`), red when the masked-context condition is inverted on run <RUN-RED> — all on build 34578327006.
```

Then `gh issue close 44 -R lang315/camoufox --comment "$(cat /tmp/c44.md)"` — **only after** Task 6 has opened the #44 PR so `#<44-PR>` can be filled in. (Execute this step's close as the last action of Task 6 for the #44 branch; the issue text is prepared here.)

---

### Task 5: Remove the 49 dot-prefixed families and turn the probe into a guard (#101)

**Branch:** `git checkout -b fix/101-dot-names meas/101-dot-names`

**Files:**
- Modify: `pythonlib/camoufox/fonts.json:6` (deletions only)
- Modify: `.github/workflows/smoke.yml` — the `# #101 MEASUREMENT` block inside the step starting at line 602 (find by the `DOT101 =` line)

**Interfaces:**
- Consumes: `mac_list_probe(js, arg, marker)`, `PIXEL_JS`, `tripwires`, `unmeasured_arms` (all defined earlier in the same step).
- Produces: `fonts.json['mac']` with 525 entries; a `(dot101)` tripwire.

- [ ] **Step 1: Remove the entries, preserving the file's single-line layout**

```bash
python3 - <<'PY'
import json
from pathlib import Path
p = Path('pythonlib/camoufox/fonts.json')
lines = p.read_text().split('\n')
i = next(n for n, l in enumerate(lines) if l.startswith('\t"mac": [')) + 1
orig = lines[i]
names = json.loads('[' + orig.strip() + ']')
assert len(names) == 574, len(names)
# Round-trip guard: the writer must reproduce the original line byte-for-byte
# before it is trusted to write the shortened one.
rebuilt = '\t\t' + ', '.join(json.dumps(n) for n in names)
assert rebuilt == orig, "writer does not reproduce the original layout"
keep = [n for n in names if not n.startswith('.')]
removed = [n for n in names if n.startswith('.')]
assert len(keep) == 525 and len(removed) == 49, (len(keep), len(removed))
lines[i] = '\t\t' + ', '.join(json.dumps(n) for n in keep)
p.write_text('\n'.join(lines))
print('removed', len(removed)); print('\n'.join(removed))
PY
python3 -c "import json; d=json.load(open('pythonlib/camoufox/fonts.json')); print({k:len(v) for k,v in d.items()})"
git diff --stat pythonlib/camoufox/fonts.json   # 1 file, 1 insertion, 1 deletion
git diff pythonlib/camoufox/fonts.json | grep -c '^-' # 2 (the --- header and the one line)
```

Expected: `{'win': 107, 'mac': 525, 'lin': 134}`. Save the printed 49 names for the PR body.

- [ ] **Step 2: Turn the probe into a deferred guard**

Replace the `# #101 MEASUREMENT (diagnostic, no assert)` comment block and the two `print` loops with:

```python
          # #101 GUARD: dot-prefixed family names must NOT be addressable by
          # name in a mac-list context. Real macOS hides them from
          # CTFontManagerCopyAvailableFontFamilyNames() and Gecko refuses them
          # by name in LookupLocalFont (CoreTextFontList.cpp:1425), so a page
          # asking for ".LastResort" on a real Mac gets the generic fallback.
          # Run 34681622483 (build 34578327006) measured all five below
          # resolving by name here -- 49 such entries in fonts.json['mac'] were
          # a by-name enumeration tell. They are removed; this guards that.
          # The launch is CAMOU_CONFIG={} and the context carries FONTS["mac"],
          # so what is measured is the per-context list alone.
          # Deferred through tripwires, never asserted inline (lesson 9).
          DOT101 = [".LastResort", ".SF NS", ".PingFang HK", ".Aqua Kana", ".Helvetica LT MM"]
          _dp = mac_list_probe(PIXEL_JS, DOT101 + ["__CamouAbsentRef__0001", "Helvetica Neue"], "d")
          _dabs = _dp["__CamouAbsentRef__0001"]["pixels"]
          _dctl = _dp["Helvetica Neue"]["pixels"] != _dabs
          print(f"  [#101 dot-name guard] absent={_dabs} control(Helvetica Neue)={_dp['Helvetica Neue']['pixels']} "
                f"control_differs={_dctl}")
          _dotlive = []
          for _f in DOT101:
              _d = _dp[_f]["pixels"] != _dabs
              print(f"  [#101 dot-name guard] {_f!r}: checksum={_dp[_f]['pixels']} differs_from_absent={_d}")
              if _d:
                  _dotlive.append(_f)
          if not _dctl:
              unmeasured_arms["(dot101)"] = (
                  "the positive control (Helvetica Neue) matches the absent reference, "
                  "so this block cannot tell resolved from unresolved")
          elif _dotlive:
              tripwires.append(
                  f"(dot101) dot-prefixed families resolve BY NAME in a mac-list context "
                  f"-- a by-name enumeration tell real macOS does not have: {_dotlive}")
```

Then `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/smoke.yml')); print('yaml ok')"` and confirm `grep -n 'DOT101 =' .github/workflows/smoke.yml` sits **after** `tripwires = []` (line 1277) and `unmeasured_arms = {}` (line 1287).

- [ ] **Step 3: Commit**

```bash
git add pythonlib/camoufox/fonts.json .github/workflows/smoke.yml
git commit -m "fix(pythonlib): drop the 49 dot-prefixed families from the mac font list (#101)

Real macOS Firefox never enumerates a dot-prefixed family:
CTFontManagerCopyAvailableFontFamilyNames() -- the API Gecko builds its list
from (CoreTextFontList.cpp:1179, :1247) -- returns none on a real host, and
LookupLocalFont refuses the names outright (:1425). fonts.json['mac'] carried
49 of them, and run 34681622483 (build 34578327006) measured five of the five
probed resolving BY NAME in a mac-list context, each to a distinct face. A
page enumerating by name saw 49 families a real Mac would never report.

Deletions only; 574 -> 525. _ESSENTIAL_FONTS_MACOS has no dot-prefixed name,
so only the random 30-78% non-essential sample changes. The writer reproduces
the original single-line layout byte-for-byte before writing the shortened
one, so the diff is one line.

The measurement probe becomes a guard: any of the five resolving by name is a
tripwire, triaged at the end of the step, never asserted inline.

The trade-off, stated: a mac-list context can no longer reach .LastResort in
fallback, which real macOS can. Enumeration by name (49 names) is the tell
being closed; the fallback glyph shape of one face is the one being accepted.
The bare no-list context is unaffected either way -- it consults no list."
git push -u origin fix/101-dot-names
```

- [ ] **Step 4: Full smoke on the branch**

```bash
gh workflow run smoke.yml -R lang315/camoufox --ref fix/101-dot-names -f run_id=34578327006
```

After it completes, read back (same extraction as Task 3, keys `#101 dot-name guard`, `#95 cmap discriminator`, `cmap95`, `dot101`, `U+FFFD fallback observed`, `UNEXPECTED RED`):

- conclusion `success`;
- all five `[#101 dot-name guard]` lines `differs_from_absent=False` with `control_differs=True` — the measurement inverted by the fix;
- zero `(dot101)` and zero `(cmap95)` lines emitted;
- the four `#95 cmap discriminator` lines still `True`;
- `arm j2 ... ctx=6 resolved=.LastResort` **unchanged** (bare context, no list — expected; say so in the PR).

If any other arm moved, that is a finding for the PR body — every arm loads `FONTS` from the edited file. Record the run id → build → branch in the ledger.

---

### Task 6: PRs, trial merge, final review, merge, close-out

**Files:** none in the repo (PR bodies, issue comments).

- [ ] **Step 1: Trial-merge the two smoke.yml branches**

```bash
git worktree add -q --detach /tmp/wt-trial origin/main
cd /tmp/wt-trial
git merge --no-commit --no-ff origin/fix/102-bundle-version -q && git commit -qm t1
git merge --no-commit --no-ff origin/fix/44-contract-guard -q && git commit -qm t2
git merge --no-commit --no-ff origin/fix/101-dot-names 2>&1 | tail -2; git diff --name-only --diff-filter=U
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/smoke.yml')); print('merged yaml ok')"
grep -c "#44 contract guard\|(dot101)" .github/workflows/smoke.yml   # expect 2 (one each)
cd - && git worktree remove --force /tmp/wt-trial
```

Expected: no `U` files, yaml ok. If a conflict appears, resolve it on `fix/101-dot-names` (the later branch) and re-run its smoke.

- [ ] **Step 2: Open the three PRs**

Each body carries: what and why; for every run quoted, `run → build 34578327006 → branch fix/95-cmap-unicode-ucs4`; the `[launch]` line for every font measurement; command output with exit status for tests; "what this does not cover". Titles: `fix(pythonlib): make the Playwright-floor warning fire for packaged builds` → **Fixes #102**; `test(smoke): guard the one-browser-per-OS contract, blocking` → **Refs #44** (closed by comment, not by keyword, so the decision text is on the issue); `fix(pythonlib): drop the 49 dot-prefixed families from the mac font list` → **Fixes #101**. The #44 body must include the precheck lines showing `lin=['Arimo', 'Tinos', 'Noto Sans JP']`, both green run ids with their `spoofed_os`, and the quoted red. The #101 body must list the 49 names and state the `.LastResort` fallback trade-off and that arm j2's bare-context line is unchanged by design.

- [ ] **Step 3: Final whole-branch review**

Three review packages (`scripts/review-package $(git merge-base origin/main origin/<branch>) origin/<branch>`), one reviewer on the most capable model, the ledger's Minor findings handed over for triage. Findings are fixed in one dispatch, re-reviewed, then:

- [ ] **Step 4: Merge in order, then close #44**

```bash
gh pr merge <102> -R lang315/camoufox --merge --delete-branch
gh pr merge <44>  -R lang315/camoufox --merge --delete-branch
gh pr merge <101> -R lang315/camoufox --merge --delete-branch
git push origin --delete meas/101-dot-names
gh issue close 44 -R lang315/camoufox --comment "$(cat /tmp/c44.md)"   # filled in from Task 4
gh workflow run smoke.yml -R lang315/camoufox --ref main -f run_id=34578327006   # post-merge confirmation
```

Expected on the confirmation run: job `success`; the #44 contract guard green with its three precheck lines; zero `(dot101)` / `(cmap95)` lines; integration suite and detector smoke ran. Record the merge shas and the confirmation run id in the ledger.
