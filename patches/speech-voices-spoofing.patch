diff --git a/dom/base/SpeechVoicesManager.cpp b/dom/base/SpeechVoicesManager.cpp
new file mode 100644
index 0000000000..09a01b3302
--- /dev/null
+++ b/dom/base/SpeechVoicesManager.cpp
@@ -0,0 +1,75 @@
+/* Camoufox: Per-context speech voices manager */
+#include "SpeechVoicesManager.h"
+
+#include "mozilla/dom/BrowsingContext.h"
+#include "mozilla/Mutex.h"
+#include "nsGlobalWindowInner.h"
+#include "xpcpublic.h"
+
+namespace mozilla {
+namespace dom {
+
+static mozilla::Mutex sVoicesMutex("SpeechVoicesManager");
+static nsTHashMap<nsUint32HashKey, nsTHashSet<nsString>> sVoicesMap;
+// #149: per process, for the reason FontListManager's sDisabledIds gives --
+// the voice list is per process, so the setter must stay reachable in every
+// process that renders the context. The per-window seal keeps page script off.
+static nsTHashSet<uint32_t> sDisabledIds;
+
+void SpeechVoicesManager::SetVoices(uint32_t aUserContextId,
+                                     const nsAString& aVoiceList) {
+  MutexAutoLock lock(sVoicesMutex);
+  nsTHashSet<nsString>& voiceSet = sVoicesMap.LookupOrInsert(aUserContextId);
+  voiceSet.Clear();
+
+  // Parse comma-separated voice names
+  nsAutoString remaining(aVoiceList);
+  while (!remaining.IsEmpty()) {
+    int32_t comma = remaining.FindChar(',');
+    nsAutoString name;
+    if (comma >= 0) {
+      name = Substring(remaining, 0, comma);
+      remaining = Substring(remaining, comma + 1);
+    } else {
+      name = remaining;
+      remaining.Truncate();
+    }
+    name.Trim(" \t");
+    if (!name.IsEmpty()) {
+      voiceSet.Insert(name);
+    }
+  }
+}
+
+bool SpeechVoicesManager::HasVoices(uint32_t aUserContextId) {
+  MutexAutoLock lock(sVoicesMutex);
+  return sVoicesMap.Contains(aUserContextId);
+}
+
+bool SpeechVoicesManager::IsVoiceAllowed(uint32_t aUserContextId,
+                                          const nsAString& aVoiceName) {
+  MutexAutoLock lock(sVoicesMutex);
+  auto* voiceSet = sVoicesMap.Lookup(aUserContextId).DataPtrOrNull();
+  if (!voiceSet) return true;
+  return voiceSet->Contains(nsString(aVoiceName));
+}
+
+void SpeechVoicesManager::DisableFunction(uint32_t aId) {
+  MutexAutoLock lock(sVoicesMutex);
+  sDisabledIds.Insert(aId);
+}
+
+bool SpeechVoicesManager::IsFunctionEnabledForWebIDL(JSContext* aCx,
+                                                      JSObject* aObj) {
+  nsGlobalWindowInner* win = xpc::WindowOrNull(aObj);
+  if (!win) return false;
+  uint32_t id = 0;
+  if (BrowsingContext* bc = win->GetBrowsingContext()) {
+    id = bc->OriginAttributesRef().mUserContextId;
+  }
+  MutexAutoLock lock(sVoicesMutex);
+  return !sDisabledIds.Contains(id);
+}
+
+}  // namespace dom
+}  // namespace mozilla
diff --git a/dom/base/SpeechVoicesManager.h b/dom/base/SpeechVoicesManager.h
new file mode 100644
index 0000000000..90e1ca117c
--- /dev/null
+++ b/dom/base/SpeechVoicesManager.h
@@ -0,0 +1,27 @@
+/* Camoufox: Per-context speech voices manager */
+#ifndef mozilla_dom_SpeechVoicesManager_h
+#define mozilla_dom_SpeechVoicesManager_h
+
+#include "nsString.h"
+#include "nsTHashMap.h"
+#include "nsHashKeys.h"
+#include "nsTHashSet.h"
+#include "RoverfoxStorageManager.h"
+
+namespace mozilla {
+namespace dom {
+
+class SpeechVoicesManager {
+ public:
+  static void SetVoices(uint32_t aUserContextId, const nsAString& aVoiceList);
+  static bool HasVoices(uint32_t aUserContextId);
+  static bool IsVoiceAllowed(uint32_t aUserContextId,
+                              const nsAString& aVoiceName);
+  static void DisableFunction(uint32_t aId);
+  static bool IsFunctionEnabledForWebIDL(JSContext* aCx, JSObject* aObj);
+};
+
+}  // namespace dom
+}  // namespace mozilla
+
+#endif  // mozilla_dom_SpeechVoicesManager_h
diff --git a/dom/base/moz.build b/dom/base/moz.build
index c92e53bb16..29872af308 100644
--- a/dom/base/moz.build
+++ b/dom/base/moz.build
@@ -564,6 +564,14 @@ EXPORTS.mozilla.dom += [
     "FontListManager.h",
 ]
 
+# SpeechVoicesManager compiled separately to avoid unified build scope issues
+# (includes RoverfoxStorageManager.h which can affect compilation units)
+SOURCES += ["SpeechVoicesManager.cpp"]
+
+EXPORTS.mozilla.dom += [
+    "SpeechVoicesManager.h",
+]
+
 # Are we targeting x86-32 or x86-64?  If so, we want to include SSE2 code for
 # CharacterDataBuffer.cpp
 if CONFIG["INTEL_ARCHITECTURE"]:
diff --git a/dom/base/nsGlobalWindowInner.cpp b/dom/base/nsGlobalWindowInner.cpp
index 165e2395fc..4ad9a66f11 100644
--- a/dom/base/nsGlobalWindowInner.cpp
+++ b/dom/base/nsGlobalWindowInner.cpp
@@ -19,6 +19,7 @@
 #include "AudioChannelService.h"
 #include "AutoplayPolicy.h"
 #include "Crypto.h"
+#include "SpeechVoicesManager.h"
 #include "MainThreadUtils.h"
 #include "Navigator.h"
 #include "PaintWorkletImpl.h"
@@ -7992,6 +7993,25 @@ void nsGlobalWindowInner::SetScreenColorDepth(uint32_t depth,
   }
 }
 
+void nsGlobalWindowInner::SetSpeechVoices(const nsAString& aVoices,
+                                           ErrorResult& aRv) {
+  uint32_t userContextId = 0;
+  if (BrowsingContext* bc = GetBrowsingContext()) {
+    userContextId = bc->OriginAttributesRef().mUserContextId;
+  }
+  SpeechVoicesManager::SetVoices(userContextId, aVoices);
+  SpeechVoicesManager::DisableFunction(userContextId);
+  // Self-destruct: set to undefined first to bust IC/shape caches on ARM64.
+  if (JSContext* cx = nsContentUtils::GetCurrentJSContext()) {
+    JS::Rooted<JSObject*> global(cx, JS::CurrentGlobalOrNull(cx));
+    if (global) {
+      JS::Rooted<JS::Value> undef(cx, JS::UndefinedValue());
+      JS_SetProperty(cx, global, "setSpeechVoices", undef);
+      JS_DeleteProperty(cx, global, "setSpeechVoices");
+    }
+  }
+}
+
 RefPtr<GenericPromise> nsGlobalWindowInner::StorageAccessPermissionChanged(
     bool aGranted) {
   // Invalidate cached StorageAllowed field so that calls to GetLocalStorage
diff --git a/dom/base/nsGlobalWindowInner.h b/dom/base/nsGlobalWindowInner.h
index c77e26b216..2eba43e141 100644
--- a/dom/base/nsGlobalWindowInner.h
+++ b/dom/base/nsGlobalWindowInner.h
@@ -709,6 +709,9 @@ class nsGlobalWindowInner final : public mozilla::dom::EventTarget,
                            mozilla::ErrorResult& aRv);
   void SetScreenColorDepth(uint32_t depth, mozilla::ErrorResult& aRv);
 
+  // Per-context speech voices
+  void SetSpeechVoices(const nsAString& aVoices, mozilla::ErrorResult& aRv);
+
  public:
   void Alert(nsIPrincipal& aSubjectPrincipal, mozilla::ErrorResult& aError);
   void Alert(const nsAString& aMessage, nsIPrincipal& aSubjectPrincipal,
diff --git a/dom/media/webspeech/synth/SpeechSynthesis.cpp b/dom/media/webspeech/synth/SpeechSynthesis.cpp
index 0bb806013f..aa04aba2ad 100644
--- a/dom/media/webspeech/synth/SpeechSynthesis.cpp
+++ b/dom/media/webspeech/synth/SpeechSynthesis.cpp
@@ -11,6 +11,8 @@
 #include "mozilla/dom/WindowGlobalChild.h"
 #include "nsContentUtils.h"
 #include "nsGlobalWindowInner.h"
+#include "mozilla/dom/BrowsingContext.h"
+#include "SpeechVoicesManager.h"
 #include "nsIDocShell.h"
 #include "nsISupportsPrimitives.h"
 #include "nsSpeechTask.h"
@@ -220,6 +222,17 @@ void SpeechSynthesis::OnEnd(const nsSpeechTask* aTask) {
 void SpeechSynthesis::GetVoices(
     nsTArray<RefPtr<SpeechSynthesisVoice> >& aResult) {
   aResult.Clear();
+  // Per-context speech voice filtering
+  uint32_t speechVoiceCtxId = 0;
+  {
+    nsCOMPtr<nsPIDOMWindowInner> ownerWin = GetOwnerWindow();
+    if (ownerWin) {
+      nsGlobalWindowInner* gwin = nsGlobalWindowInner::Cast(ownerWin);
+      if (BrowsingContext* bc = gwin->GetBrowsingContext()) {
+        speechVoiceCtxId = bc->OriginAttributesRef().mUserContextId;
+      }
+    }
+  }
   uint32_t voiceCount = 0;
   nsCOMPtr<nsPIDOMWindowInner> window = GetOwnerWindow();
   nsCOMPtr<nsIDocShell> docShell = window ? window->GetDocShell() : nullptr;
@@ -245,6 +258,16 @@ void SpeechSynthesis::GetVoices(
       continue;
     }
 
+    // Per-context voice filtering: skip voices not in allowed list
+    if (mozilla::dom::SpeechVoicesManager::HasVoices(speechVoiceCtxId)) {
+      nsAutoString voiceName;
+      nsSynthVoiceRegistry::GetInstance()->GetVoiceName(uri, voiceName);
+      if (!mozilla::dom::SpeechVoicesManager::IsVoiceAllowed(
+              speechVoiceCtxId, voiceName)) {
+        continue;
+      }
+    }
+
     SpeechSynthesisVoice* voice = mVoiceCache.GetWeak(uri);
 
     if (!voice) {
diff --git a/dom/media/webspeech/synth/moz.build b/dom/media/webspeech/synth/moz.build
index e1bf345159..ca6ffacb21 100644
--- a/dom/media/webspeech/synth/moz.build
+++ b/dom/media/webspeech/synth/moz.build
@@ -59,5 +59,6 @@ include("/ipc/chromium/chromium-config.mozbuild")
 
 FINAL_LIBRARY = "xul"
 LOCAL_INCLUDES += [
+    "/dom/base",
     "ipc",
 ]
diff --git a/dom/webidl/Window.webidl b/dom/webidl/Window.webidl
index b7fbdd6201..d33bab01bc 100644
--- a/dom/webidl/Window.webidl
+++ b/dom/webidl/Window.webidl
@@ -952,6 +952,12 @@ partial interface Window {
   undefined setFontList(DOMString fontList);
 };
 
+// Per-context speech voices
+partial interface Window {
+  [Throws, Func="mozilla::dom::SpeechVoicesManager::IsFunctionEnabledForWebIDL"]
+  undefined setSpeechVoices(DOMString voices);
+};
+
 // Used to assign marks to appear on the scrollbar when
 // finding on a page.
 partial interface Window {
